var list = ['tiger', 'cat', 'bear', 'bird'];
console.log(list);

list.shift();    //  remove first item in the list
console.log(list);
list.pop();       //remove last item in the list
console.log(list);
list.push('elephant')      //add new  item in the last of the list
console.log(list);

list = list.concat(["turtle", "rabbit" ])        // add multiple items
console.log(list);

list.sort();
console.log(list);      